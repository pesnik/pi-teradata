---
description: Kick off the full plan -> implement -> review chain for a schema/ETL change
---

Run the `etl-change` chain (see agents/agent-chain.yaml) for this request:

{{args}}

Do not skip the dba-reviewer step even if the change looks trivial.
